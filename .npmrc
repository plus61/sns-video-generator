# Railway platform-specific settings
# Ensure correct platform binaries are installed
platform=linux
arch=x64
libc=glibc

# Disable optional dependencies that might cause issues
omit=optional

# Ensure production dependencies only
production=true