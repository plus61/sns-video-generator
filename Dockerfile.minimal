# 最もシンプルな構成
FROM node:18-alpine

WORKDIR /app

# 必要なツール
RUN apk add --no-cache libc6-compat

# ファイルをコピー
COPY . .

# 依存関係をインストール
RUN npm install

# ビルドをスキップして直接開発モードで起動
EXPOSE 3000

CMD ["npm", "run", "dev"]